import { tokens } from './tokens.foundation';

export const palettes = (() => {
  const lightTransparent = 'rgba(245, 245, 245, 0)';
  const lightPalette = [
    lightTransparent,
    tokens.color.tomato1 as unknown as string,
    tokens.color.tomato2 as unknown as string,
    tokens.color.tomato3 as unknown as string,
    tokens.color.tomato4 as unknown as string,
    tokens.color.tomato5 as unknown as string,
    tokens.color.tomato6 as unknown as string,
    tokens.color.tomato7 as unknown as string,
    tokens.color.tomato8 as unknown as string,
    tokens.color.tomato9 as unknown as string,
    tokens.color.tomato10 as unknown as string,
    tokens.color.tomato11 as unknown as string,
    tokens.color.tomato12 as unknown as string,
    tokens.color.red1 as unknown as string,
    tokens.color.red2 as unknown as string,
    tokens.color.red3 as unknown as string,
    tokens.color.red4 as unknown as string,
    tokens.color.red5 as unknown as string,
    tokens.color.red6 as unknown as string,
    tokens.color.red7 as unknown as string,
    tokens.color.red8 as unknown as string,
    tokens.color.red9 as unknown as string,
    tokens.color.red10 as unknown as string,
    tokens.color.red11 as unknown as string,
    tokens.color.red12 as unknown as string,
    tokens.color.crimson1 as unknown as string,
    tokens.color.crimson2 as unknown as string,
    tokens.color.crimson3 as unknown as string,
    tokens.color.crimson4 as unknown as string,
    tokens.color.crimson5 as unknown as string,
    tokens.color.crimson6 as unknown as string,
    tokens.color.crimson7 as unknown as string,
    tokens.color.crimson8 as unknown as string,
    tokens.color.crimson9 as unknown as string,
    tokens.color.crimson10 as unknown as string,
    tokens.color.crimson11 as unknown as string,
    tokens.color.crimson12 as unknown as string,
    tokens.color.pink1 as unknown as string,
    tokens.color.pink2 as unknown as string,
    tokens.color.pink3 as unknown as string,
    tokens.color.pink4 as unknown as string,
    tokens.color.pink5 as unknown as string,
    tokens.color.pink6 as unknown as string,
    tokens.color.pink7 as unknown as string,
    tokens.color.pink8 as unknown as string,
    tokens.color.pink9 as unknown as string,
    tokens.color.pink10 as unknown as string,
    tokens.color.pink11 as unknown as string,
    tokens.color.pink12 as unknown as string,
    tokens.color.plum1 as unknown as string,
    tokens.color.plum2 as unknown as string,
    tokens.color.plum3 as unknown as string,
    tokens.color.plum4 as unknown as string,
    tokens.color.plum5 as unknown as string,
    tokens.color.plum6 as unknown as string,
    tokens.color.plum7 as unknown as string,
    tokens.color.plum8 as unknown as string,
    tokens.color.plum9 as unknown as string,
    tokens.color.plum10 as unknown as string,
    tokens.color.plum11 as unknown as string,
    tokens.color.plum12 as unknown as string,
    tokens.color.purple1 as unknown as string,
    tokens.color.purple2 as unknown as string,
    tokens.color.purple3 as unknown as string,
    tokens.color.purple4 as unknown as string,
    tokens.color.purple5 as unknown as string,
    tokens.color.purple6 as unknown as string,
    tokens.color.purple7 as unknown as string,
    tokens.color.purple8 as unknown as string,
    tokens.color.purple9 as unknown as string,
    tokens.color.purple10 as unknown as string,
    tokens.color.purple11 as unknown as string,
    tokens.color.purple12 as unknown as string,
    tokens.color.violet1 as unknown as string,
    tokens.color.violet2 as unknown as string,
    tokens.color.violet3 as unknown as string,
    tokens.color.violet4 as unknown as string,
    tokens.color.violet5 as unknown as string,
    tokens.color.violet6 as unknown as string,
    tokens.color.violet7 as unknown as string,
    tokens.color.violet8 as unknown as string,
    tokens.color.violet9 as unknown as string,
    tokens.color.violet10 as unknown as string,
    tokens.color.violet11 as unknown as string,
    tokens.color.violet12 as unknown as string,
    tokens.color.indigo1 as unknown as string,
    tokens.color.indigo2 as unknown as string,
    tokens.color.indigo3 as unknown as string,
    tokens.color.indigo4 as unknown as string,
    tokens.color.indigo5 as unknown as string,
    tokens.color.indigo6 as unknown as string,
    tokens.color.indigo7 as unknown as string,
    tokens.color.indigo8 as unknown as string,
    tokens.color.indigo9 as unknown as string,
    tokens.color.indigo10 as unknown as string,
    tokens.color.indigo11 as unknown as string,
    tokens.color.indigo12 as unknown as string,
    tokens.color.blue1 as unknown as string,
    tokens.color.blue2 as unknown as string,
    tokens.color.blue3 as unknown as string,
    tokens.color.blue4 as unknown as string,
    tokens.color.blue5 as unknown as string,
    tokens.color.blue6 as unknown as string,
    tokens.color.blue7 as unknown as string,
    tokens.color.blue8 as unknown as string,
    tokens.color.blue9 as unknown as string,
    tokens.color.blue10 as unknown as string,
    tokens.color.blue11 as unknown as string,
    tokens.color.blue12 as unknown as string,
    tokens.color.cyan1 as unknown as string,
    tokens.color.cyan2 as unknown as string,
    tokens.color.cyan3 as unknown as string,
    tokens.color.cyan4 as unknown as string,
    tokens.color.cyan5 as unknown as string,
    tokens.color.cyan6 as unknown as string,
    tokens.color.cyan7 as unknown as string,
    tokens.color.cyan8 as unknown as string,
    tokens.color.cyan9 as unknown as string,
    tokens.color.cyan10 as unknown as string,
    tokens.color.cyan11 as unknown as string,
    tokens.color.cyan12 as unknown as string,
    tokens.color.teal1 as unknown as string,
    tokens.color.teal2 as unknown as string,
    tokens.color.teal3 as unknown as string,
    tokens.color.teal4 as unknown as string,
    tokens.color.teal5 as unknown as string,
    tokens.color.teal6 as unknown as string,
    tokens.color.teal7 as unknown as string,
    tokens.color.teal8 as unknown as string,
    tokens.color.teal9 as unknown as string,
    tokens.color.teal10 as unknown as string,
    tokens.color.teal11 as unknown as string,
    tokens.color.teal12 as unknown as string,
    tokens.color.green1 as unknown as string,
    tokens.color.green2 as unknown as string,
    tokens.color.green3 as unknown as string,
    tokens.color.green4 as unknown as string,
    tokens.color.green5 as unknown as string,
    tokens.color.green6 as unknown as string,
    tokens.color.green7 as unknown as string,
    tokens.color.green8 as unknown as string,
    tokens.color.green9 as unknown as string,
    tokens.color.green10 as unknown as string,
    tokens.color.green11 as unknown as string,
    tokens.color.green12 as unknown as string,
    tokens.color.grass1 as unknown as string,
    tokens.color.grass2 as unknown as string,
    tokens.color.grass3 as unknown as string,
    tokens.color.grass4 as unknown as string,
    tokens.color.grass5 as unknown as string,
    tokens.color.grass6 as unknown as string,
    tokens.color.grass7 as unknown as string,
    tokens.color.grass8 as unknown as string,
    tokens.color.grass9 as unknown as string,
    tokens.color.grass10 as unknown as string,
    tokens.color.grass11 as unknown as string,
    tokens.color.grass12 as unknown as string,
    tokens.color.orange1 as unknown as string,
    tokens.color.orange2 as unknown as string,
    tokens.color.orange3 as unknown as string,
    tokens.color.orange4 as unknown as string,
    tokens.color.orange5 as unknown as string,
    tokens.color.orange6 as unknown as string,
    tokens.color.orange7 as unknown as string,
    tokens.color.orange8 as unknown as string,
    tokens.color.orange9 as unknown as string,
    tokens.color.orange10 as unknown as string,
    tokens.color.orange11 as unknown as string,
    tokens.color.orange12 as unknown as string,
    tokens.color.brown1 as unknown as string,
    tokens.color.brown2 as unknown as string,
    tokens.color.brown3 as unknown as string,
    tokens.color.brown4 as unknown as string,
    tokens.color.brown5 as unknown as string,
    tokens.color.brown6 as unknown as string,
    tokens.color.brown7 as unknown as string,
    tokens.color.brown8 as unknown as string,
    tokens.color.brown9 as unknown as string,
    tokens.color.brown10 as unknown as string,
    tokens.color.brown11 as unknown as string,
    tokens.color.brown12 as unknown as string,
    tokens.color.sky1 as unknown as string,
    tokens.color.sky2 as unknown as string,
    tokens.color.sky3 as unknown as string,
    tokens.color.sky4 as unknown as string,
    tokens.color.sky5 as unknown as string,
    tokens.color.sky6 as unknown as string,
    tokens.color.sky7 as unknown as string,
    tokens.color.sky8 as unknown as string,
    tokens.color.sky9 as unknown as string,
    tokens.color.sky10 as unknown as string,
    tokens.color.sky11 as unknown as string,
    tokens.color.sky12 as unknown as string,
    tokens.color.mint1 as unknown as string,
    tokens.color.mint2 as unknown as string,
    tokens.color.mint3 as unknown as string,
    tokens.color.mint4 as unknown as string,
    tokens.color.mint5 as unknown as string,
    tokens.color.mint6 as unknown as string,
    tokens.color.mint7 as unknown as string,
    tokens.color.mint8 as unknown as string,
    tokens.color.mint9 as unknown as string,
    tokens.color.mint10 as unknown as string,
    tokens.color.mint11 as unknown as string,
    tokens.color.mint12 as unknown as string,
    tokens.color.lime1 as unknown as string,
    tokens.color.lime2 as unknown as string,
    tokens.color.lime3 as unknown as string,
    tokens.color.lime4 as unknown as string,
    tokens.color.lime5 as unknown as string,
    tokens.color.lime6 as unknown as string,
    tokens.color.lime7 as unknown as string,
    tokens.color.lime8 as unknown as string,
    tokens.color.lime9 as unknown as string,
    tokens.color.lime10 as unknown as string,
    tokens.color.lime11 as unknown as string,
    tokens.color.lime12 as unknown as string,
    tokens.color.yellow1 as unknown as string,
    tokens.color.yellow2 as unknown as string,
    tokens.color.yellow3 as unknown as string,
    tokens.color.yellow4 as unknown as string,
    tokens.color.yellow5 as unknown as string,
    tokens.color.yellow6 as unknown as string,
    tokens.color.yellow7 as unknown as string,
    tokens.color.yellow8 as unknown as string,
    tokens.color.yellow9 as unknown as string,
    tokens.color.yellow10 as unknown as string,
    tokens.color.yellow11 as unknown as string,
    tokens.color.yellow12 as unknown as string,
    tokens.color.amber1 as unknown as string,
    tokens.color.amber2 as unknown as string,
    tokens.color.amber3 as unknown as string,
    tokens.color.amber4 as unknown as string,
    tokens.color.amber5 as unknown as string,
    tokens.color.amber6 as unknown as string,
    tokens.color.amber7 as unknown as string,
    tokens.color.amber8 as unknown as string,
    tokens.color.amber9 as unknown as string,
    tokens.color.amber10 as unknown as string,
    tokens.color.amber11 as unknown as string,
    tokens.color.amber12 as unknown as string,
    tokens.color.gray1 as unknown as string,
    tokens.color.gray2 as unknown as string,
    tokens.color.gray3 as unknown as string,
    tokens.color.gray4 as unknown as string,
    tokens.color.gray5 as unknown as string,
    tokens.color.gray6 as unknown as string,
    tokens.color.gray7 as unknown as string,
    tokens.color.gray8 as unknown as string,
    tokens.color.gray9 as unknown as string,
    tokens.color.gray10 as unknown as string,
    tokens.color.gray11 as unknown as string,
    tokens.color.gray12 as unknown as string,
    tokens.color.mauve1 as unknown as string,
    tokens.color.mauve2 as unknown as string,
    tokens.color.mauve3 as unknown as string,
    tokens.color.mauve4 as unknown as string,
    tokens.color.mauve5 as unknown as string,
    tokens.color.mauve6 as unknown as string,
    tokens.color.mauve7 as unknown as string,
    tokens.color.mauve8 as unknown as string,
    tokens.color.mauve9 as unknown as string,
    tokens.color.mauve10 as unknown as string,
    tokens.color.mauve11 as unknown as string,
    tokens.color.mauve12 as unknown as string,
    tokens.color.slate1 as unknown as string,
    tokens.color.slate2 as unknown as string,
    tokens.color.slate3 as unknown as string,
    tokens.color.slate4 as unknown as string,
    tokens.color.slate5 as unknown as string,
    tokens.color.slate6 as unknown as string,
    tokens.color.slate7 as unknown as string,
    tokens.color.slate8 as unknown as string,
    tokens.color.slate9 as unknown as string,
    tokens.color.slate10 as unknown as string,
    tokens.color.slate11 as unknown as string,
    tokens.color.slate12 as unknown as string,
    tokens.color.olive1 as unknown as string,
    tokens.color.olive2 as unknown as string,
    tokens.color.olive3 as unknown as string,
    tokens.color.olive4 as unknown as string,
    tokens.color.olive5 as unknown as string,
    tokens.color.olive6 as unknown as string,
    tokens.color.olive7 as unknown as string,
    tokens.color.olive8 as unknown as string,
    tokens.color.olive9 as unknown as string,
    tokens.color.olive10 as unknown as string,
    tokens.color.olive11 as unknown as string,
    tokens.color.olive12 as unknown as string,
    tokens.color.sand1 as unknown as string,
    tokens.color.sand2 as unknown as string,
    tokens.color.sand3 as unknown as string,
    tokens.color.sand4 as unknown as string,
    tokens.color.sand5 as unknown as string,
    tokens.color.sand6 as unknown as string,
    tokens.color.sand7 as unknown as string,
    tokens.color.sand8 as unknown as string,
    tokens.color.sand9 as unknown as string,
    tokens.color.sand10 as unknown as string,
    tokens.color.sand11 as unknown as string,
    tokens.color.sand12 as unknown as string,
    tokens.color.gold1 as unknown as string,
    tokens.color.gold2 as unknown as string,
    tokens.color.gold3 as unknown as string,
    tokens.color.gold4 as unknown as string,
    tokens.color.gold5 as unknown as string,
    tokens.color.gold6 as unknown as string,
    tokens.color.gold7 as unknown as string,
    tokens.color.gold8 as unknown as string,
    tokens.color.gold9 as unknown as string,
    tokens.color.gold10 as unknown as string,
    tokens.color.gold11 as unknown as string,
    tokens.color.gold12 as unknown as string,
    tokens.color.bronze1 as unknown as string,
    tokens.color.bronze2 as unknown as string,
    tokens.color.bronze3 as unknown as string,
    tokens.color.bronze4 as unknown as string,
    tokens.color.bronze5 as unknown as string,
    tokens.color.bronze6 as unknown as string,
    tokens.color.bronze7 as unknown as string,
    tokens.color.bronze8 as unknown as string,
    tokens.color.bronze9 as unknown as string,
    tokens.color.bronze10 as unknown as string,
    tokens.color.bronze11 as unknown as string,
    tokens.color.bronze12 as unknown as string,
    tokens.color.whiteA1 as unknown as string,
    tokens.color.whiteA2 as unknown as string,
    tokens.color.whiteA3 as unknown as string,
    tokens.color.whiteA4 as unknown as string,
    tokens.color.whiteA5 as unknown as string,
    tokens.color.whiteA6 as unknown as string,
    tokens.color.whiteA7 as unknown as string,
    tokens.color.whiteA8 as unknown as string,
    tokens.color.whiteA9 as unknown as string,
    tokens.color.whiteA10 as unknown as string,
    tokens.color.whiteA11 as unknown as string,
    tokens.color.whiteA12 as unknown as string,
    tokens.color.blackA1 as unknown as string,
    tokens.color.blackA2 as unknown as string,
    tokens.color.blackA3 as unknown as string,
    tokens.color.blackA4 as unknown as string,
    tokens.color.blackA5 as unknown as string,
    tokens.color.blackA6 as unknown as string,
    tokens.color.blackA7 as unknown as string,
    tokens.color.blackA8 as unknown as string,
    tokens.color.blackA9 as unknown as string,
    tokens.color.blackA10 as unknown as string,
    tokens.color.blackA11 as unknown as string,
    tokens.color.blackA12 as unknown as string,
  ];

  return {
    light: lightPalette,
  };
})();
